
# cajal-cfg
This crate handles dynamic compile-time configuration for the `cajal` library,
and is used in the library's `build.rs` to find the configuration variables.

`neuron.cfg` contains the default hyperparameters used by `cajal`, 
and can be used as a template for creating custom configuration files.

